apiVersion: v1
kind: Service
metadata:
  name: recommendation-engine
  labels:
    app: recommendation-engine
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: recommendation-engine
